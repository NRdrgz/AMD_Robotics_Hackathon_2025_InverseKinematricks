[project]
name = "system"
version = "0.1.0"
description = "Package sorting system with two SO101 arms"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lerobot[smolvla]==0.4.1",
    "websockets>=12.0",
    "httpx>=0.27.0",
    "pyserial>=3.5",
]
